@model IEnumerable<Victuz_MVC.Models.Enrollment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Schrijf je in!</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Tijd van inschrijving
            </th>
            <th>
                Activiteit
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EnrolledAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activity.Name)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn">Aanpassen</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn-delete">Uitschrijven</a>
            </td>
        </tr>
}
    </tbody>
</table>
